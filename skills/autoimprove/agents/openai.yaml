interface:
  display_name: "Improve AI Prompt"
  short_description: "Iterate against explicit evaluation results"
  default_prompt: "Improve this skill or prompt through bounded measure-change-test iterations."
policy:
  allow_implicit_invocation: false
