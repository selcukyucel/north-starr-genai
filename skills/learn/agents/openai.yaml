interface:
  display_name: "Record Project Learning"
  short_description: "Capture reusable patterns and landmines"
  default_prompt: "Record this verified project learning without duplicating existing guidance."
policy:
  allow_implicit_invocation: false
